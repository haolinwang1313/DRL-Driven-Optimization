# Data

This directory records local input-data placement. The current revision expects the benchmark spreadsheet at:

```text
data/external/benchmark/dataset.xlsx
```

The spreadsheet is not committed while redistribution rights are unclear. Keep catalog metadata in `data/catalog.yaml` and do not delete the only local copy when reorganizing files.
