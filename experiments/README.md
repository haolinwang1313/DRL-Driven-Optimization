# Experiments

This directory stores historical experiment notes and run definitions. It does not store generated result bundles; those remain under ignored `artifacts/` paths.

The migrated logbook is `logbook.md`.
